/**
 * Usage tips surfaced to users, graded by experience level.
 *
 * Every tip names a real command, tool, or config key — verified against the
 * command registry, the outbound tool schemas, and `types.ts` rather than
 * written from memory. A tip that names something AgEnD does not have is worse
 * than no tip: it sends people looking for a feature and erodes trust in the
 * rest of the list. When a capability is backend-specific (`/btw` is Claude
 * Code only), the tip says so rather than implying it works everywhere.
 *
 * The Chinese text is written natively, not translated from the English — the
 * two convey the same fact, and neither reads as a translation of the other.
 */
export interface Tip {
  id: string;
  level: "beginner" | "intermediate" | "advanced";
  text_en: string;
  text_zh: string;
}

export const TIPS: Tip[] = [
  // ── Beginner (30) ─────────────────────────────────────────────────────
  { id: "tip-001", level: "beginner",
    text_en: "Each topic is one agent. Type in a topic and that agent reads it — no prefix, no mention needed.",
    text_zh: "一個 topic 就是一個 agent。直接在 topic 裡打字它就收到了，不用加前綴也不用 @。" },
  { id: "tip-002", level: "beginner",
    text_en: "General is the dispatcher: describe what you want and it picks or creates the right instance instead of you doing it by hand.",
    text_zh: "General 是調度員。你只要說想做什麼，它會挑或開一台合適的 instance，不必自己一台台指定。" },
  { id: "tip-003", level: "beginner",
    text_en: "While an agent is working a Cancel button sits under its bubble — tap it to stop the current turn.",
    text_zh: "agent 工作時泡泡下面會有取消鈕，按一下就中斷這一輪。" },
  { id: "tip-004", level: "beginner",
    text_en: "When the turn ends the Cancel button goes away. If tools ran, the bubble stays as a 🧾 tool-history record.",
    text_zh: "一輪結束取消鈕就消失。如果中間跑過工具，泡泡會留下來變成 🧾 工具歷程紀錄。" },
  { id: "tip-005", level: "beginner",
    text_en: "Run `/ctx` to see how full the agent's context is before handing it a big task.",
    text_zh: "丟大任務前先 `/ctx` 看一下 context 還剩多少。" },
  { id: "tip-006", level: "beginner",
    text_en: "`/clear` wipes the session and starts clean — the fastest fix when an agent is stuck on a stale assumption.",
    text_zh: "`/clear` 清掉 session 重新開始 —— agent 卡在舊的錯誤前提時，這是最快的解法。" },
  { id: "tip-007", level: "beginner",
    text_en: "`/compact` shrinks the context but keeps the thread going. Use it instead of `/clear` when the history still matters.",
    text_zh: "`/compact` 壓縮 context 但保住對話。歷史還有用的時候用它，不要用 `/clear`。" },
  { id: "tip-008", level: "beginner",
    text_en: "`/usage` shows how much of your Claude / Codex / Grok / Kiro subscription is left.",
    text_zh: "`/usage` 看 Claude / Codex / Grok / Kiro 的訂閱額度還剩多少。" },
  { id: "tip-009", level: "beginner",
    text_en: "`/model` shows the model actually in force — including one inherited from fleet defaults — and switches it (admin only).",
    text_zh: "`/model` 會顯示真正生效的模型（包含從 fleet defaults 繼承來的），也可以直接換（限管理員）。" },
  { id: "tip-010", level: "beginner",
    text_en: "`/status` tells you whether an instance is running, paused, stopped, or crashed. Check it before assuming a message was ignored.",
    text_zh: "`/status` 看 instance 是 running、paused、stopped 還是 crashed。覺得訊息被無視時先查這個。" },
  { id: "tip-011", level: "beginner",
    text_en: "`/restart` respawns the CLI while keeping the instance's config and topic. It is the right first move for a wedged agent.",
    text_zh: "`/restart` 會重啟 CLI，但保留 instance 的設定和 topic。agent 卡死時先試這個。" },
  { id: "tip-012", level: "beginner",
    text_en: "`/pause` only works on an idle instance — pause frees its slot without losing the session.",
    text_zh: "`/pause` 只能對 idle 的 instance 用。暫停會釋放它的名額，但 session 不會掉。" },
  { id: "tip-013", level: "beginner",
    text_en: "A paused instance wakes by itself when a message arrives, so you rarely need `/wake` by hand.",
    text_zh: "暫停的 instance 收到訊息會自己醒，所以很少需要手動 `/wake`。" },
  { id: "tip-014", level: "beginner",
    text_en: "`/cancel` stops the running turn from the keyboard — same effect as the Cancel button.",
    text_zh: "`/cancel` 用鍵盤中斷目前那一輪，效果跟按取消鈕一樣。" },
  { id: "tip-015", level: "beginner",
    text_en: "`/effort` sets reasoning depth: low, medium, high, xhigh, or max. Raise it for hard problems, lower it for routine edits.",
    text_zh: "`/effort` 調推理深度：low、medium、high、xhigh、max。難題調高，例行修改調低。" },
  { id: "tip-016", level: "beginner",
    text_en: "`/update` upgrades AgEnD in place. Run it when a release note mentions a fix you have been waiting for.",
    text_zh: "`/update` 原地升級 AgEnD。看到 release note 有你在等的修復就跑它。" },
  { id: "tip-017", level: "beginner",
    text_en: "`/doctor` runs the built-in diagnostics — start there when something is broken but no error is visible.",
    text_zh: "`/doctor` 跑內建診斷。壞了但看不到錯誤訊息時從這裡開始查。" },
  { id: "tip-018", level: "beginner",
    text_en: "`/dashboard` gives you the web view: live terminal, usage, and settings in one page.",
    text_zh: "`/dashboard` 開網頁介面 —— 即時終端、額度、設定都在一頁。" },
  { id: "tip-019", level: "beginner",
    text_en: "Attach a photo or file to your message and the agent reads it directly. No need to paste a path.",
    text_zh: "訊息直接夾圖片或檔案，agent 會自己讀。不用貼路徑。" },
  { id: "tip-020", level: "beginner",
    text_en: "Telegram voice messages work — record instead of typing when you are away from a keyboard.",
    text_zh: "Telegram 語音訊息可以用。手邊沒鍵盤時直接錄，不必打字。" },
  { id: "tip-021", level: "beginner",
    text_en: "Reply to a specific message and the agent sees what you quoted, which is the cheapest way to give it context.",
    text_zh: "用「回覆」引用某則訊息，agent 看得到你引的內容 —— 這是給它上下文最省事的方法。" },
  { id: "tip-022", level: "beginner",
    text_en: "The reaction on your message is delivery status: 👀 seen, ⏳ queued, ✅ delivered, ❌ failed.",
    text_zh: "你訊息上的表情就是投遞狀態：👀 已看到、⏳ 排隊中、✅ 已送達、❌ 失敗。" },
  { id: "tip-023", level: "beginner",
    text_en: "Do not resend while an agent is working — the message is queued, not lost. Use `/steer` if you need to add something now.",
    text_zh: "agent 在忙的時候不要重送 —— 訊息是排隊不是掉了。真的要補話就用 `/steer`。" },
  { id: "tip-024", level: "beginner",
    text_en: "AgEnD does not validate model names; it passes them to the CLI. A typo therefore fails at launch, not when you set it.",
    text_zh: "AgEnD 不檢查模型名稱，直接丟給 CLI。所以打錯字是在啟動時才爆，不是設定的當下。" },
  { id: "tip-025", level: "beginner",
    text_en: "Ask General to run `list_models` before you name a model — it shows what the backend actually offers.",
    text_zh: "要指定模型前先請 General 跑 `list_models`，它會列出那個 backend 真正有的模型。" },
  { id: "tip-026", level: "beginner",
    text_en: "If a login expires, AgEnD pauses that backend's instances and tells you once. Logging in once restores all of them.",
    text_zh: "登入過期時 AgEnD 會暫停那個 backend 的 instance，並且只通知一次。登入一次全部就恢復。" },
  { id: "tip-027", level: "beginner",
    text_en: "Leaving the model unset is usually right: it inherits the fleet default, or the CLI's own, which stays current as vendors ship new models.",
    text_zh: "模型不設通常才是對的：它會繼承 fleet default 或 CLI 自己的預設，廠商推新模型時自動跟上。" },
  { id: "tip-028", level: "beginner",
    text_en: "`/sysinfo` prints host and version details — paste it when you report a problem so nobody has to ask.",
    text_zh: "`/sysinfo` 印出主機和版本資訊。回報問題時附上，省掉一輪來回。" },
  { id: "tip-029", level: "beginner",
    text_en: "Add `@` to a command in a shared channel (`/ctx@name`) to aim it at one specific instance.",
    text_zh: "在共用頻道裡指令加 `@`（例如 `/ctx@name`）就能指定某一台 instance。" },
  { id: "tip-030", level: "beginner",
    text_en: "One agent per topic means you can talk to several in parallel — they do not share context or interrupt each other.",
    text_zh: "一個 topic 一個 agent，所以可以同時跟好幾台講話 —— 它們不共用 context，也不會互相打斷。" },

  // ── Intermediate (40) ─────────────────────────────────────────────────
  { id: "tip-031", level: "intermediate",
    text_en: "`/steer <message>` interjects into a turn already in progress — use it to correct course instead of cancelling and restarting.",
    text_zh: "`/steer <訊息>` 可以插話進正在跑的那一輪。想修正方向就用它，不必取消重來。" },
  { id: "tip-032", level: "intermediate",
    text_en: "`/steer` needs a CLI that accepts input mid-turn; on backends whose TUI cannot, AgEnD tells you rather than dropping the message.",
    text_zh: "`/steer` 需要 CLI 支援中途輸入。TUI 做不到的 backend，AgEnD 會明講而不是把訊息吞掉。" },
  { id: "tip-033", level: "intermediate",
    text_en: "`/btw <message>` asks a side question without interrupting the current task. Claude Code only — other backends will say no.",
    text_zh: "`/btw <訊息>` 問個插題但不打斷目前任務。**只有 Claude Code 支援**，其他 backend 會直接拒絕。" },
  { id: "tip-034", level: "intermediate",
    text_en: "`/raw` (admin only) forwards your text to the agent untouched — useful when AgEnD's own parsing gets in the way.",
    text_zh: "`/raw`（限管理員）把文字原封不動轉給 agent。AgEnD 自己的解析礙事時很有用。" },
  { id: "tip-035", level: "intermediate",
    text_en: "`delegate_task` hands work to another instance; the assignee works silently and answers with `report_result`.",
    text_zh: "`delegate_task` 把工作交給另一台 instance。對方會安靜做完，再用 `report_result` 回報。" },
  { id: "tip-036", level: "intermediate",
    text_en: "Use `request_information` when you need context from another instance instead of guessing at its repo.",
    text_zh: "需要另一台的資訊時用 `request_information`，不要去猜它的 repo 長什麼樣。" },
  { id: "tip-037", level: "intermediate",
    text_en: "Always return the `correlation_id` you were given in `report_result` — that is what pairs your answer with the request.",
    text_zh: "`report_result` 一定要帶回收到的 `correlation_id`，那是把回答對上請求的依據。" },
  { id: "tip-038", level: "intermediate",
    text_en: "Instances talk to each other directly with `send_to_instance`. Routing everything through a coordinator just adds a hop.",
    text_zh: "instance 之間用 `send_to_instance` 直接講。全部繞過協調者只是多一個轉手。" },
  { id: "tip-039", level: "intermediate",
    text_en: "An \"IPC request timed out\" error does not mean the message failed — it usually already arrived. Check the log before resending.",
    text_zh: "看到「IPC request timed out」不代表沒送到 —— 通常其實已經到了。重送前先查 log。" },
  { id: "tip-040", level: "intermediate",
    text_en: "`broadcast` reaches several instances at once; give it an explicit target list so it does not wake everything.",
    text_zh: "`broadcast` 一次通知多台。記得給明確的目標清單，不然會把全部都叫醒。" },
  { id: "tip-041", level: "intermediate",
    text_en: "Run `describe_instance` before `create_instance` — reusing a fitting instance beats spawning a near-duplicate.",
    text_zh: "開新的之前先 `describe_instance`。有現成合適的就重用，別開一台幾乎一樣的。" },
  { id: "tip-042", level: "intermediate",
    text_en: "`create_instance` without a directory auto-creates a workspace under `~/.agend/workspaces/<name>`.",
    text_zh: "`create_instance` 不給目錄時，會自動在 `~/.agend/workspaces/<name>` 開一個工作區。" },
  { id: "tip-043", level: "intermediate",
    text_en: "The fleet lives in `~/.agend/fleet.yaml`. `update_instance_config` and `update_fleet_defaults` edit it for you and save.",
    text_zh: "fleet 設定在 `~/.agend/fleet.yaml`。用 `update_instance_config` 和 `update_fleet_defaults` 改，它會幫你存檔。" },
  { id: "tip-044", level: "intermediate",
    text_en: "Run `validate_config` after editing fleet.yaml by hand — catching a typo there beats finding it during a restart.",
    text_zh: "手改 fleet.yaml 之後跑 `validate_config`。在這裡抓到打錯字，比重啟時才發現好得多。" },
  { id: "tip-045", level: "intermediate",
    text_en: "`create_schedule` takes exactly one of `cron` (recurring) or `at` (one-shot ISO time) — never both.",
    text_zh: "`create_schedule` 只能給 `cron`（週期）或 `at`（一次性 ISO 時間）其中一個，不能兩個都給。" },
  { id: "tip-046", level: "intermediate",
    text_en: "A silent schedule (`silent: true`) injects straight into the agent's pane without posting to the channel — right for heartbeats.",
    text_zh: "靜默排程（`silent: true`）直接注入 agent 的 pane，不會發到頻道 —— 適合心跳類的任務。" },
  { id: "tip-047", level: "intermediate",
    text_en: "Schedules default to Asia/Taipei. Pass an IANA `timezone` if that is not where you are.",
    text_zh: "排程預設時區是 Asia/Taipei。不在這個時區就自己帶 IANA `timezone`。" },
  { id: "tip-048", level: "intermediate",
    text_en: "`create_schedule` with a `target` fires into another instance — a general can schedule work for a worker.",
    text_zh: "`create_schedule` 帶 `target` 可以排到別台去 —— general 能替 worker 安排工作。" },
  { id: "tip-049", level: "intermediate",
    text_en: "`list_schedules`, `update_schedule`, `delete_schedule` manage what is already booked; `enabled: false` pauses one without deleting it.",
    text_zh: "已排的用 `list_schedules`、`update_schedule`、`delete_schedule` 管理。`enabled: false` 可以停用但不刪掉。" },
  { id: "tip-050", level: "intermediate",
    text_en: "Use the `task` board for multi-step work so others can read your progress instead of asking for a status update.",
    text_zh: "多步驟工作用 `task` 看板記錄，別人自己看進度就好，不用來問你。" },
  { id: "tip-051", level: "intermediate",
    text_en: "`create_team` groups instances that work together; `list_teams` is the first call in the documented dispatch flow.",
    text_zh: "`create_team` 把一起工作的 instance 編成一組。標準分派流程的第一步就是 `list_teams`。" },
  { id: "tip-052", level: "intermediate",
    text_en: "`post_decision` shares an architectural choice fleet-wide, so the next instance does not re-litigate it.",
    text_zh: "`post_decision` 把架構決策公告到全 fleet，下一台就不會再重新吵一遍。" },
  { id: "tip-053", level: "intermediate",
    text_en: "Run `list_decisions` after a restart to reload the fleet's shared decisions into your context.",
    text_zh: "重啟後跑一次 `list_decisions`，把全 fleet 的共同決策重新載進 context。" },
  { id: "tip-054", level: "intermediate",
    text_en: "When a decision is superseded, `update_decision` it — a stale decision quietly misleads every new instance.",
    text_zh: "決策被推翻時用 `update_decision` 更新。過時的決策會默默誤導每一台新 instance。" },
  { id: "tip-055", level: "intermediate",
    text_en: "`get_instance_logs` reads the tail of another instance's output — faster than asking it what went wrong.",
    text_zh: "`get_instance_logs` 直接讀另一台的輸出尾巴，比問它「剛才怎麼了」快。" },
  { id: "tip-056", level: "intermediate",
    text_en: "`get_fleet_status` gives running / paused / stopped counts in one call — use it before deciding to spawn anything.",
    text_zh: "`get_fleet_status` 一次拿到 running / paused / stopped 的數量。決定要不要開新的之前先看它。" },
  { id: "tip-057", level: "intermediate",
    text_en: "`tool_progress` controls the tool list under a working bubble: \"standard\" for semantic labels, \"verbose\" to add command previews, \"off\" (the default) for none.",
    text_zh: "`tool_progress` 控制工作泡泡下的工具清單：`standard` 顯示語意標籤、`verbose` 再加上指令預覽、`off`（預設）則完全不顯示。" },
  { id: "tip-058", level: "intermediate",
    text_en: "`defaults.progress_min_elapsed` controls how long before the bubble starts showing elapsed time — raise it if short turns feel noisy.",
    text_zh: "`defaults.progress_min_elapsed` 決定泡泡多久後才開始顯示已耗時。短任務覺得吵就調高。" },
  { id: "tip-059", level: "intermediate",
    text_en: "`agend export-chat --from 09:00 --to 18:00 -o day.html` turns fleet activity into a shareable HTML log.",
    text_zh: "`agend export-chat --from 09:00 --to 18:00 -o day.html` 把 fleet 活動輸出成可分享的 HTML 紀錄。" },
  { id: "tip-060", level: "intermediate",
    text_en: "`/chat save <name>` snapshots a kiro session and `/chat load <name>` restores it — handy before a risky experiment.",
    text_zh: "`/chat save <名稱>` 存下 kiro session，`/chat load <名稱>` 還原。要做危險實驗前先存一份。" },
  { id: "tip-061", level: "intermediate",
    text_en: "`auto_pause_after` pauses an instance after it sits idle, which stops warm agents burning tokens on nothing.",
    text_zh: "`auto_pause_after` 讓 instance 閒置一段時間後自動暫停，避免熱著的 agent 空燒 token。" },
  { id: "tip-062", level: "intermediate",
    text_en: "`tool_set` trims which MCP tools an instance sees. Every tool's schema is resent on every API call, so a smaller set is cheaper.",
    text_zh: "`tool_set` 可以縮減 instance 看到的 MCP 工具。每次 API 呼叫都會重送所有工具的 schema，工具越少越省。" },
  { id: "tip-063", level: "intermediate",
    text_en: "`get_effort` reports the level in force, the levels that backend accepts, and whether a change needs a restart.",
    text_zh: "`get_effort` 會告訴你目前生效的等級、那個 backend 支援哪些等級，以及改動要不要重啟。" },
  { id: "tip-064", level: "intermediate",
    text_en: "`/backend` switches an instance's CLI. The session does not carry across backends, so expect a fresh start.",
    text_zh: "`/backend` 換 instance 的 CLI。session 不會跨 backend 帶過去，換完等於重新開始。" },
  { id: "tip-065", level: "intermediate",
    text_en: "`set_display_name` and `set_description` make an instance legible to others — an unnamed instance gets the wrong work sent to it.",
    text_zh: "`set_display_name` 和 `set_description` 讓別人看懂這台在幹嘛。沒名字的 instance 會被派錯工作。" },
  { id: "tip-066", level: "intermediate",
    text_en: "`replace_instance` swaps in a fresh CLI while keeping the topic and config — the move for a session poisoned beyond `/clear`.",
    text_zh: "`replace_instance` 換上乾淨的 CLI 但保留 topic 和設定。session 壞到 `/clear` 救不回來時用它。" },
  { id: "tip-067", level: "intermediate",
    text_en: "In the web view, Settings edits the same fleet.yaml the tools do — no need to SSH in to change a backend.",
    text_zh: "網頁介面的 Settings 改的就是同一份 fleet.yaml。要換 backend 不必 SSH 進機器。" },
  { id: "tip-068", level: "intermediate",
    text_en: "`get_fleet_config` with `scope: \"instance\"` shows one instance's effective config, including what it inherited.",
    text_zh: "`get_fleet_config` 帶 `scope: \"instance\"` 會顯示單一 instance 的實際生效設定，包含繼承來的部分。" },
  { id: "tip-069", level: "intermediate",
    text_en: "Never send an acknowledgement-only message between instances. Silence means working; only send content.",
    text_zh: "instance 之間不要發純確認訊息。沒消息就是在做，只發有內容的訊息。" },
  { id: "tip-070", level: "intermediate",
    text_en: "Batch review feedback into one message. Two round trips is the target; a third should cover only what is unresolved.",
    text_zh: "review 意見一次講完。目標是兩個往返，真的需要第三次就只談還沒解決的部分。" },

  // ── Advanced (30) ─────────────────────────────────────────────────────
  { id: "tip-071", level: "advanced",
    text_en: "`backend_options: { codex: { provider: \"glm\" } }` points a Codex instance at a custom provider without touching the others.",
    text_zh: "`backend_options: { codex: { provider: \"glm\" } }` 讓某台 Codex 走自訂 provider，不影響其他台。" },
  { id: "tip-072", level: "advanced",
    text_en: "Pass `instance_name` to `list_models` for a custom-provider instance: its catalog comes from its own CODEX_HOME and can differ from the account's.",
    text_zh: "自訂 provider 的 instance 要用 `list_models` 帶 `instance_name` —— 它的 catalog 來自自己的 CODEX_HOME，可能跟帳號的不同。" },
  { id: "tip-073", level: "advanced",
    text_en: "Check the `scope` field in a `list_models` reply: \"instance\" is authoritative for that instance, \"global\" is only the account-wide list.",
    text_zh: "看 `list_models` 回傳的 `scope`：`instance` 對那台是權威答案，`global` 只是帳號層級的清單。" },
  { id: "tip-074", level: "advanced",
    text_en: "Define a `templates` section in fleet.yaml, then `deploy_template` stands up the whole set of instances in one call.",
    text_zh: "在 fleet.yaml 定義 `templates`，之後一次 `deploy_template` 就把整組 instance 建起來。" },
  { id: "tip-075", level: "advanced",
    text_en: "Give `deploy_template` a `branch` and each instance gets its own git worktree from it — parallel work with no checkout fights.",
    text_zh: "`deploy_template` 帶 `branch` 的話，每台會從它切出自己的 git worktree —— 平行開發不會互搶 checkout。" },
  { id: "tip-076", level: "advanced",
    text_en: "`list_deployments` shows what a template created, and `teardown_deployment` removes the whole group at once.",
    text_zh: "`list_deployments` 看 template 建了什麼，`teardown_deployment` 一次收掉整組。" },
  { id: "tip-077", level: "advanced",
    text_en: "An instance on a worktree records its origin in `worktree_source` — that is how you tell a worktree from a plain checkout.",
    text_zh: "跑在 worktree 上的 instance 會把來源記在 `worktree_source`，用它區分 worktree 和普通 checkout。" },
  { id: "tip-078", level: "advanced",
    text_en: "`checkout_repo` and `release_repo` lease a repo to one instance so two agents never write the same tree at once.",
    text_zh: "`checkout_repo` 和 `release_repo` 把 repo 租給單一 instance，避免兩個 agent 同時寫同一棵樹。" },
  { id: "tip-079", level: "advanced",
    text_en: "`defaults.warm_cap` caps how many instances stay warm; over the cap, the least-recently-active idle one is auto-paused.",
    text_zh: "`defaults.warm_cap` 限制同時熱著的 instance 數量。超過上限時，最久沒動的閒置那台會被自動暫停。" },
  { id: "tip-080", level: "advanced",
    text_en: "`warm_cap` is a count and `auto_pause_after` is a clock — they are complementary, so set both on a busy fleet.",
    text_zh: "`warm_cap` 管數量，`auto_pause_after` 管時間，兩者互補。忙碌的 fleet 兩個都要設。" },
  { id: "tip-081", level: "advanced",
    text_en: "`warm_cap` eviction never touches a general instance — your dispatcher stays up even when the fleet is over its cap.",
    text_zh: "warm_cap 的淘汰永遠不會動到 general。就算 fleet 超過上限，調度員也會留著。" },
  { id: "tip-082", level: "advanced",
    text_en: "`context_guardian.max_age_hours` recycles a session before it rots; `grace_period_ms` keeps it from firing mid-task.",
    text_zh: "`context_guardian.max_age_hours` 在 session 腐化前先回收，`grace_period_ms` 避免它在任務中途觸發。" },
  { id: "tip-083", level: "advanced",
    text_en: "Set `mirror_topic_id` to get a read-only topic that mirrors every cross-instance message — the cheapest way to watch a fleet talk.",
    text_zh: "設 `mirror_topic_id` 會得到一個唯讀 topic，鏡射所有跨 instance 訊息 —— 觀察 fleet 對話最省事的方法。" },
  { id: "tip-084", level: "advanced",
    text_en: "`hang_detector` catches an agent that stopped making progress without crashing; tune `timeout_minutes` to your slowest real task.",
    text_zh: "`hang_detector` 抓那種沒 crash 但停止推進的 agent。`timeout_minutes` 要照你最慢的真實任務調。" },
  { id: "tip-085", level: "advanced",
    text_en: "`restart_policy` sets `max_retries`, `backoff`, and `reset_after` — without a sane `reset_after` a flaky CLI looks like a crash loop.",
    text_zh: "`restart_policy` 設定 `max_retries`、`backoff`、`reset_after`。`reset_after` 沒設好的話，偶發不穩的 CLI 會被誤判成 crash loop。" },
  { id: "tip-086", level: "advanced",
    text_en: "`model_failover: [\"opus\", \"sonnet\"]` is an ordered fallback list — the instance drops to the next model when the primary hits a rate limit instead of stalling.",
    text_zh: "`model_failover: [\"opus\", \"sonnet\"]` 是有順序的備援清單。主模型被限流時就往下一個掉，不會卡住。" },
  { id: "tip-087", level: "advanced",
    text_en: "`cost_guard` caps spend per instance, overriding the fleet default — useful for an experiment you do not want to babysit.",
    text_zh: "`cost_guard` 可以單台限制花費、覆蓋 fleet 預設。跑不想盯著的實驗時很有用。" },
  { id: "tip-088", level: "advanced",
    text_en: "`pre_task_command` pastes a command before every user message — e.g. reloading a saved session on each turn.",
    text_zh: "`pre_task_command` 會在每則使用者訊息前先貼一個指令，例如每輪都重新載入存好的 session。" },
  { id: "tip-089", level: "advanced",
    text_en: "`kiro_ui` picks the kiro profile (`legacy`, `tui`, `v3`). Pane-scraped signals such as context % differ between them.",
    text_zh: "`kiro_ui` 選 kiro 的介面模式（`legacy`、`tui`、`v3`）。靠 pane 抓的訊號（例如 context %）在不同模式下不一樣。" },
  { id: "tip-090", level: "advanced",
    text_en: "`terminal.columns` / `terminal.rows` pin the tmux geometry an agent sees — widen it when a CLI truncates its own tables.",
    text_zh: "`terminal.columns` / `terminal.rows` 固定 agent 看到的 tmux 尺寸。CLI 自己把表格截斷時就調寬。" },
  { id: "tip-091", level: "advanced",
    text_en: "`agent_mode: \"cli\"` swaps the MCP transport for an HTTP endpoint — the escape hatch for a CLI whose MCP support is unreliable.",
    text_zh: "`agent_mode: \"cli\"` 把 MCP 傳輸換成 HTTP endpoint。CLI 的 MCP 支援不穩時用這條後路。" },
  { id: "tip-092", level: "advanced",
    text_en: "MCP is a stdio protocol owned by the CLI, so a dead MCP server cannot be respawned mid-session — the instance needs a restart to get its tools back.",
    text_zh: "MCP 是由 CLI 掌握 pipe 的 stdio 協定，所以 MCP server 死掉沒辦法中途重生 —— 要重啟 instance 才會拿回工具。" },
  { id: "tip-093", level: "advanced",
    text_en: "An instance that answers normally but cannot reply, report, or delegate has lost its MCP transport — a restart is the fix, not prompt debugging.",
    text_zh: "一台看起來會正常回話、但沒辦法 reply/report/delegate 的 instance，是掉了 MCP 連線。解法是重啟，不是去查 prompt。" },
  { id: "tip-094", level: "advanced",
    text_en: "A `general_topic` instance needs the `general` tool profile — `standard` lacks delegate_task and create_instance, so dispatch silently cannot work.",
    text_zh: "`general_topic` 的 instance 必須用 `general` 工具組。`standard` 少了 delegate_task 和 create_instance，分派會默默做不到。" },
  { id: "tip-095", level: "advanced",
    text_en: "`channel.access.mode: locked` plus `allowed_users` restricts who can drive the fleet. An empty admin list means nobody is admin — a secure default, not a bug.",
    text_zh: "`channel.access.mode: locked` 加 `allowed_users` 限制誰能操作 fleet。管理員清單留空表示沒人是管理員 —— 那是安全預設，不是 bug。" },
  { id: "tip-096", level: "advanced",
    text_en: "`startup.concurrency` and `stagger_delay_ms` spread a fleet restart out, so a dozen CLIs do not contend for CPU at once.",
    text_zh: "`startup.concurrency` 和 `stagger_delay_ms` 把 fleet 重啟攤開，避免十幾個 CLI 同時搶 CPU。" },
  { id: "tip-097", level: "advanced",
    text_en: "`tags: [\"code-reviewer\", \"researcher\"]` lets a general route by capability instead of memorising instance names.",
    text_zh: "`tags: [\"code-reviewer\", \"researcher\"]` 讓 general 照能力路由，不必記 instance 名字。" },
  { id: "tip-098", level: "advanced",
    text_en: "Fleet-level `project_roots` lists the directories your repos live in, so agents can find a project instead of being told its full path each time.",
    text_zh: "fleet 層的 `project_roots` 列出你的 repo 放在哪些目錄，agent 就能自己找到專案，不必每次都告訴它完整路徑。" },
  { id: "tip-099", level: "advanced",
    text_en: "`lightweight: true` skips the transcript monitor, context guardian, approval server, and prompt detector — right for an instance that only relays or watches.",
    text_zh: "`lightweight: true` 會跳過 transcript monitor、context guardian、approval server 和 prompt detector —— 適合只負責轉發或監看的 instance。" },
  { id: "tip-100", level: "advanced",
    text_en: "`mcp_auto_restart` (default true) restarts an instance whose MCP server died, waiting for an idle pane first and resuming the session rather than resetting it.",
    text_zh: "`mcp_auto_restart`（預設開啟）會在 MCP server 死掉時自動重啟該 instance —— 先等 pane 閒置，而且是續接 session 不是清掉重來。" },
];
